# Anthropic — server-side only, never exposed to the client
ANTHROPIC_API_KEY=

# Postgres (Neon/Supabase). Integration tests use an ephemeral DB.
DATABASE_URL=

# Auth (Clerk or Supabase) — keys added in M2+
# CLERK_SECRET_KEY=
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
